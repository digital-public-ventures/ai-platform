[pytest]
testpaths = .
addopts = -q -p no:anyio -p no:asyncio -p no:docker -p no:langsmith
filterwarnings =
    ignore:'crypt' is deprecated:DeprecationWarning
    ignore:Support for class-based `config` is deprecated:DeprecationWarning
log_cli = false

